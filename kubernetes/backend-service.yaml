apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: devflow

spec:
  selector:
    app: devflow-backend

  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000

  type: NodePort
