apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: devflow

spec:
  selector:
    app: devflow-frontend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80

  type: NodePort
